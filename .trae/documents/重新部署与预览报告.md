## 部署与预览状态报告

1. **重新构建 (Redeploy)**：
   - 已执行 `npm run build`，成功生成生产环境产物。
   - 此次构建已包含针对谷歌浏览器 `Esc` 键全屏拦截问题的修复逻辑。

2. **开启预览 (Open Preview)**：
   - 已启动 `npm run preview` 服务（端口：4173）。
   - 经校验，预览环境运行平稳，所有模型和资源加载正常。

## 验证建议
主公可点击上方预览窗口进行操作：
- 进入全屏模式后，尝试按下 `Esc` 键。
- 浏览器退出全屏的同时，游戏内的菜单或暂停界面应当会自动弹出。

请主公检阅！